import loot.GameFrameSettings;

public class Program {

	public static void main(String[] args) {
		int mode = 2;

		if (mode == 0) {
			GameFrameSettings settings = new GameFrameSettings();

			SampleFrame1_ArrayList window = new SampleFrame1_ArrayList(settings);
			window.setVisible(true);
		}
		
		if (mode == 1) {
			GameFrameSettings settings = new GameFrameSettings();

			SampleFrame2_Viewport window = new SampleFrame2_Viewport(settings);
			window.setVisible(true);
		}		
		
		if (mode == 2) {
			GameFrameSettings settings = new GameFrameSettings();

			SampleFrame3_Layer window = new SampleFrame3_Layer(settings);
			window.setVisible(true);
		}		
	}

}
